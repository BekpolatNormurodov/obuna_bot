generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model Movie {
  id        Int      @id @default(autoincrement())
  title     String
  fileId    String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

enum ChannelType {
  TELEGRAM
  INSTAGRAM
}

model Channel {
  id        Int         @id @default(autoincrement())
  type      ChannelType @default(TELEGRAM)
  chatId    String?     @unique
  username  String?
  title     String
  inviteUrl String?
  createdAt DateTime    @default(now())
}

model InstagramFollow {
  id        Int      @id @default(autoincrement())
  channelId Int
  userId    BigInt
  confirmed Boolean  @default(false)
  createdAt DateTime @default(now())

  @@unique([channelId, userId])
}

enum PendingStep {
  AWAITING_CHOICE
  WAITING_TITLE
  WAITING_EDIT_NUMBER
  CONFIRM_REPLACE
}

model PendingUpload {
  id            Int         @id @default(autoincrement())
  adminId       BigInt      @unique
  fileId        String
  caption       String?
  step          PendingStep
  targetMovieId Int?
  createdAt     DateTime    @default(now())
}

enum ChannelActionStep {
  WAITING_TYPE
  WAITING_TELEGRAM_IDENTIFIER
  WAITING_INSTAGRAM_LINK
  WAITING_INSTAGRAM_TITLE
}

model PendingChannelAction {
  id              Int               @id @default(autoincrement())
  adminId         BigInt            @unique
  step            ChannelActionStep
  targetChannelId Int?
  draftLink       String?
  createdAt       DateTime          @default(now())
}

model BotUser {
  id         Int      @id @default(autoincrement())
  telegramId BigInt   @unique
  createdAt  DateTime @default(now())
}

model PendingBroadcast {
  id        Int      @id @default(autoincrement())
  adminId   BigInt   @unique
  createdAt DateTime @default(now())
}

enum BroadcastStatus {
  DRAFT
  SENT
}

model Broadcast {
  id              Int             @id @default(autoincrement())
  adminId         BigInt
  sourceChatId    BigInt
  sourceMessageId Int
  status          BroadcastStatus @default(DRAFT)
  sentCount       Int             @default(0)
  failedCount     Int             @default(0)
  createdAt       DateTime        @default(now())
}
